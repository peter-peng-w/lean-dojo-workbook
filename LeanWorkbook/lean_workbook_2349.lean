import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_2349 : ∀ a b : ℝ, 6 * (a ^ 2 + b ^ 2) ^ 2 + (a + b) ^ 4 ≥ 5 * (a ^ 2 + b ^ 2) * (a + b) ^ 2 := by
  sorry