import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_9826 (x y : ℝ) : 1 ≤ (x / (x + 3 * y))^(1 / 2) + (y / (y + 3 * x))^(1 / 2) := by
  sorry