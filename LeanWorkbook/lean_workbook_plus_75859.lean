import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_75859 (x y : ℝ) : (x - y) ^ 2 * (7 * x ^ 2 - 4 * x * y + 7 * y ^ 2) ≥ 0 := by
  sorry