import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_31393 (a b c : ℝ) : (a^2 / (a^2 - a * b + b^2))^(1 / 4) + (b^2 / (b^2 - b * c + c^2))^(1 / 4) + (c^2 / (c^2 - c * a + a^2))^(1 / 4) ≤ 3 := by
  sorry