import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_49947 (a b c : ℝ) : a^2 + b^2 + c^2 + 3 * (a * b + b * c + c * a) ≥ 4 * (a * b + b * c + c * a) := by
  sorry