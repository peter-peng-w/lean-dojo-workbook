import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_51362 (p : ℕ) (h₁ : Nat.Prime p) (h₂ : p > 5) (h₃ : p = 4 * k - 1) : (p + 1) / 2 = 2 * k := by
  sorry