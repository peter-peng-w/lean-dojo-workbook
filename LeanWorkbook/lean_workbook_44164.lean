import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_44164 {a b : ℝ} (ha : 0 < a) (hb : 0 < b) : (1 / a + 1 / b) / 2 ≥ 2 / (a + b) := by
  sorry