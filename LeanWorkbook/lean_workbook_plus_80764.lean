import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_80764 (a b c : ℝ) (ha : 0 < a) (hb : 0 < b) (hc : 0 < c) (hab : 2 * b^2 = a^2 + c^2) : 1 / (a + b) + 1 / (b + c) = 2 / (a + c) := by
  sorry