import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_23850 (x y z a : ℝ) (h₁ : x = a) (h₂ : 2 * a - 3 * y + z ^ 2 = 1) : y = 1 / 3 * z ^ 2 + (2 * a - 1) / 3 := by
  sorry