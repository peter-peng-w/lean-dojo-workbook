import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_24436 (a b : ℝ) (ha : 0 < a) (hb : 0 < b) (hab : a + b = 2) :(1 + a^(1/5))^5 + (1 + b^(1/5))^5 ≤ 64 := by
  sorry