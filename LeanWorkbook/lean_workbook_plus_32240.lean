import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_32240 (x z : ℂ) : x^2 = 2 * z * (x^2 / 9 + 1) ↔ x^2 = 2 * z * (x^2 / 9 + 1) := by
  sorry