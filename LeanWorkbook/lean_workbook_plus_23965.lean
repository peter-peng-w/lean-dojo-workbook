import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_23965 (x y z : ℝ) (hx : x ≥ -1) (hy : y ≥ -1) (hz : z ≥ -1) (hxy : x + y ≥ 2) (hxz : x + z ≥ 2) (hyz : y + z ≥ 2) : x * y + x * z + y * z ≥ 3 := by
  sorry