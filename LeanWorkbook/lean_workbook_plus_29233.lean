import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_theorem : 4 * (3 + 2 * a * b + 2 * a * c + 2 * b * c) ≥ 12 * (a + b + c) ↔ 3 + 2 * a * b + 2 * a * c + 2 * b * c ≥ 3 * (a + b + c) := by
  sorry