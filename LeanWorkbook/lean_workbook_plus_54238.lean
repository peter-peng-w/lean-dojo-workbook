import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_54238 : a^3 * b + b^3 * c + c^3 * a = 2 / 3 * (a^2 * b^2 + b^2 * c^2 + c^2 * a^2) → (a^2 + b^2 + c^2)^2 ≥ 4 * (a^3 * b + b^3 * c + c^3 * a) := by
  sorry