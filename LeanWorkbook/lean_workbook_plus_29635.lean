import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_29635 (x : ℝ) (hx : 1 / 2 ≤ x ∧ x ≤ 1) :
  1 - (x^(1/6) : ℝ) ≤ (1 - x) / (3 * (2^(1/6) : ℝ)) := by
  sorry