import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_7972 (a b c x y z: ℝ) : (a ^ 2 + x ^ 2) * (b ^ 2 + y ^ 2) * (c ^ 2 + z ^ 2) = (a * y * z + b * x * z + c * x * y - a * b * c) ^ 2 + (x * b * c + y * a * c + z * a * b - x * y * z) ^ 2 := by
  sorry