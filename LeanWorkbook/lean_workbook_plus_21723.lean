import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_21723 (a b c d : ℝ) (h : a + b + c + d = 0) : a^3 + b^3 + c^3 + d^3 = 3 * (a + d) * (b + d) * (c + d) := by
  sorry