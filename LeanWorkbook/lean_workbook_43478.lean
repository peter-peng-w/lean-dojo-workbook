import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_43478 (a b c : ℝ) (ha : a > 0) (hb : b > 0) (hc : c > 0) : (a^2 + b^2 + c^2)^(1/3) ≤ (2^(1/3) * ((a^2)^(1/3) + (b^2)^(1/3) + (c^2)^(1/3))) := by
  sorry