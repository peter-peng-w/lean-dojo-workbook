import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_69695 : ∀ a b c : ℝ, (8 * a - 11 * b - 7 * c) ^ 2 + 23 * (b - 3 * c) ^ 2 ≥ 0 := by
  sorry