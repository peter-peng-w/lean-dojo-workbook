import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_62182  (x y z t : ℝ) (h₀ : x = 2 * t + 1) (h₁ : y = 4 - 2 * t) (h₂ : z = 3 * t + 6) (h₃ : 0 < x ∧ 0 < y ∧ 0 < z) : -(1 / 2) < t ∧ t < 2 := by
  sorry