import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_12649 (x y z : ℝ) (hx : x ≥ 0) (hy : y ≥ 0) (hz : z ≥ 0) : x * y ^ 2 + y * z ^ 2 + z * x ^ 2 + x * y * z ≥ 0 := by
  sorry