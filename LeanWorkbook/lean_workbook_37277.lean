import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_37277 (a b c : ℝ) (ha : 0 ≤ a) (hb : 0 ≤ b) (hc : 0 ≤ c) (habc : a + b + c = 1) : (a^3 + b)^(1/3) + (b + c^3)^(1/3) ≥ (a + b^3 + c)^(1/3) := by
  sorry