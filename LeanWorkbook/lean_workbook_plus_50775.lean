import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_50775 (x y z : ℝ) (hx : x ≠ 0) (hyz : y + z ≠ 0) : (x / (y + z))^(1/3) = (2:ℝ)^(1/3) / (2 * (y / x + z / x))^(2/3) := by
  sorry