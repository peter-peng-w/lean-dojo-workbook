import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_37458 (x y z : ℝ) : x^2 + y^2 + z^2 + 3 * x * y - x * z - y * z ≥ 4 * x * y := by
  sorry