import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_12277 : ∀ a : ℝ, (a - 1) ^ 2 * (9 * a ^ 4 - 84 * a ^ 3 + 310 * a ^ 2 - 580 * a + 601) ≥ 0 := by
  sorry