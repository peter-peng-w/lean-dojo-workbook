import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_76452 (a b c : ℝ) (ha : 0 < a) (hb : 0 < b) (hc : 0 < c) (habc : a * b * c = 1) (h : a * b + b * c + c * a = (3 / 2) * (a ^ 2 + b ^ 2 + c ^ 2 - 1)) : a * b * c ≤ 1 := by
  sorry