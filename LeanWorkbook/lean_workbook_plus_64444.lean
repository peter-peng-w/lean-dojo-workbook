import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_64444 : ∀ a b : ℤ, a^5 + b^5 = (a + b) * (a^4 - a^3 * b + a^2 * b^2 - a * b^3 + b^4) := by
  sorry