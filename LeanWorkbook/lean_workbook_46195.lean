import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_46195 (a b c : ℝ) : (b * c ^ 3 + c * a ^ 3 + a * b ^ 3) ^ 2 ≥ 3 * a * b * c * (a ^ 2 * c ^ 3 + a ^ 3 * b ^ 2 + b ^ 3 * c ^ 2) := by
  sorry