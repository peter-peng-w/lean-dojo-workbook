import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_37592 (a b c : ℝ) (ha : a > 0 ∧ b > 0 ∧ c > 0)(habc : a * b * c = 1) (h : a^2 + b^2 + c^2 + 2 * a * b * c = 1) : a + b + c ≤ 3 / 2 := by
  sorry