import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_14746  (f : ℝ → ℝ)
  (h₀ : (∀ x, (f x)^2 = 2 * f x)) :
  f 2 = 0 ∨ f 2 = 2 := by
  sorry