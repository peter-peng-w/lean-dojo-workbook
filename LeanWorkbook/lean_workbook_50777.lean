import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_50777 (a b c : ℝ) (ha : 0 < a) (hb : 0 < b) (hc : 0 < c) (habc : a + b + c = 3) : b^(2 / 3) * c^(2 / 3) + c^(2 / 3) * a^(2 / 3) + a^(2 / 3) * b^(2 / 3) ≤ 3 := by
  sorry