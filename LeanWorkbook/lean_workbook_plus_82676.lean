import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_82676 (a b : ℝ) : (a^4 / 2 + 3 * a^2 * b^2 + b^4 / 2) ≥ 2 * a * b * (a^2 + b^2) := by
  sorry