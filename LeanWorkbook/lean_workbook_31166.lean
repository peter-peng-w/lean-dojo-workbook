import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_31166 (a b c : ℝ) (h : a + b + c = 3) :
  a^2 + b^2 + a^2 * b^2 + a * b * c ≥ 4 * a * b := by
  sorry