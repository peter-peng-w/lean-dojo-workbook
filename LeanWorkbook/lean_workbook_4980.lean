import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_4980 (a b c : ℝ) : (a^2 + b^2 - a * b + b * c + c * a) * (b^2 + c^2 - b * c + c * a + a * b) * (c^2 + a^2 - c * a + a * b + b * c) ≥ (a * b + b * c + c * a) * (a^2 + b^2 + c^2)^2 := by
  sorry