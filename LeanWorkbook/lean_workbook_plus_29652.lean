import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_29652  (h₀ : x = 64)
  (h₁ : y = 80)
  (h₂ : z = 24)
  (h₃ : a = 2) :
  3 * (x + y + z + a) = 510 := by
  sorry