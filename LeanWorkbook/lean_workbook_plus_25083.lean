import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_25083 {A B C : ℂ} (h : A + B + C = 0) : A ^ 3 + B ^ 3 + C ^ 3 = 3 * A * B * C := by
  sorry