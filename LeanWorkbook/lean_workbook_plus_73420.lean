import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_73420 (p q : ℤ) (hp : p = 5 * 2 - 7) (hq : q = 14) : p / q = 3 / 14 := by
  sorry