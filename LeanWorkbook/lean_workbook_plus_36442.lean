import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_36442 (n : ℕ) (hn : 0 < n) : (∃ k : ℕ, n^2 + 2^n = k^2) ↔ ∃ k : ℕ, n^2 + 2^n = k^2 ∧ k > 0 := by
  sorry