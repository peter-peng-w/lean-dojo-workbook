import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_61936 (x y : ℝ) (h₁ : x = -2) (h₂ : y = 0) (h₃ : r = 2) : π * r ^ 2 = 4 * π := by
  sorry