import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_45900 : ∀ a b c : ℝ, (a + 3 * b) ^ 2 + (3 * b + 2 * c) ^ 2 ≥ (a + 6 * b + 2 * c) ^ 2 / 2 := by
  sorry