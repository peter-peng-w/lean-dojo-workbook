import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_34538 (a b c : ℝ) : (a^2 - a * b + b^2 - (a + b) / 2)^2 + (b^2 - b * c + c^2 - (b + c) / 2)^2 + (c^2 - c * a + a^2 - (c + a) / 2)^2 ≥ 0 := by
  sorry