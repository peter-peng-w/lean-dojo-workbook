import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_53242 (m n u v : ℤ) (hm : m^2 - 87 * n + 1923 = u^2) (hn : n^2 - 87 * m + 1923 = v^2) : (m - n) * (m + n + 87) = (u - v) * (u + v) := by
  sorry