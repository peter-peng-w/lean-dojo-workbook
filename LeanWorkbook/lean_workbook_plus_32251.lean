import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_32251 (x y : ℝ) (hx : x ≠ 0) (hy : y ≠ 0) (hxy : x + 1/y = 1/5) (hxy : y + 1/x = 20) : x*y = 1 := by
  sorry