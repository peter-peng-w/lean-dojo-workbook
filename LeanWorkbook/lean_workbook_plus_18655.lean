import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_18655 : a = 7 + 6 * Complex.I ∧ b = 1 - 3 * Complex.I → b / a = (1 - 3 * Complex.I) / (7 + 6 * Complex.I) := by
  sorry