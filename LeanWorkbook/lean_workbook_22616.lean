import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_22616 (a b c : ℝ) : 3 * (a ^ 2 + b ^ 2 + c ^ 2) ^ 2 + 3 * (a + b + c) ^ 4 ≥ 6 * (a ^ 2 + b ^ 2 + c ^ 2) * (a + b + c) ^ 2 := by
  sorry