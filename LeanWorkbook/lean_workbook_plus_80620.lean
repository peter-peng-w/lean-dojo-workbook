import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_80620 {m : ℤ} : (m^2 - 1) * (m^2 + 2) = (m^2 - 1)^2 + 3 * (m^2 - 1) := by
  sorry