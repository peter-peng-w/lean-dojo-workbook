import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_26534 (x y : ℝ) : 3 * x ^ 2 + 3 * y ^ 2 + 3 * x * y + 3 * x + 3 * y + 4 ≥ 0 := by
  sorry