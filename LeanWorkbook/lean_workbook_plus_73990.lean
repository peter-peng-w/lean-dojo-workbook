import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_73990 (a b : ℤ) : a ^ 4 + 4 * b ^ 4 = (a ^ 2 + 2 * a * b + 2 * b ^ 2) * (a ^ 2 - 2 * a * b + 2 * b ^ 2) := by
  sorry