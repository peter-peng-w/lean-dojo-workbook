import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_6932 (x y z : ℝ) (hx : 0 < x) (hy : 0 < y) (hz : 0 < z) : (x ^ (5 / 3) + y ^ (5 / 3) + z ^ (5 / 3)) / 3 ≥ (x + y + z) / 3 ^ (5 / 3) := by
  sorry