import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_2793 : ∀ n : ℤ, n^4 + 64 = (n^2 + 8)^2 - 16 * n^2 ∧ (n^2 + 8)^2 - 16 * n^2 = (n^2 + 8 - 4 * n) * (n^2 + 8 + 4 * n) := by
  sorry