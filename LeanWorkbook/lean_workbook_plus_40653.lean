import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_40653 (ha : a > 0) : (a - 1) ^ 2 * (9 * a ^ 6 - 12 * a ^ 5 - 5 * a ^ 4 + 2 * a ^ 3 + 9 * a ^ 2 + 6 * a + 3) ≥ 0 := by
  sorry