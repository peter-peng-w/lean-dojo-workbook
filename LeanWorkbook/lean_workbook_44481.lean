import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_44481 {a b c d : ℝ} (hab : a > 0 ∧ b > 0) (hcd : c > 0 ∧ d > 0) : Real.log b / Real.log a * (Real.log d / Real.log c) = Real.log d / Real.log a * (Real.log b / Real.log c) := by
  sorry