import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_22312 (a b c : ℝ) (h : a^3 - b^3 = 25 * (a - b)) (h' : b^3 - c^3 = 49 * (b - c)) (h'' : c^3 - a^3 = 64 * (c - a)) : 8 * b + 5 * c = 13 * a := by
  sorry