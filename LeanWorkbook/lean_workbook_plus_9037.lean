import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_9037 :
  10! / (8! * 2!) * (4! / (2! * 2!)) * (4! / (2! * 2!)) = 1620 := by
  sorry