import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_theorem (x y : ℝ) (h₁ : x + y = 2) (h₂ : 2*x + y = 5) (h₃ : x - y = 4) : x = 3 ∧ y = -1 := by
  sorry