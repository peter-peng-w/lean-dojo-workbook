import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_70798 (h : ∃ a b c : ℝ, a ≠ 0 ∧ b ≠ 0 ∧ c ≠ 0 ∧ (a * 1 + b * 6 + c * 2 = 0 ∧ a * 5 + b * 2 + c * 1 = 0 ∧ a * 1 + b * 0 + c * (-2) = 0)) : ∃ a b c : ℝ, a ≠ 0 ∧ b ≠ 0 ∧ c ≠ 0 ∧ (a * 1 + b * 6 + c * 2 = 0 ∧ a * 5 + b * 2 + c * 1 = 0 ∧ a * 1 + b * 0 + c * (-2) = 0) := by
  sorry