import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_34281 (x y : ℝ) (hx : 0 < x) (hy : 0 < y) : x ^ 3 + y ^ 3 ≥ x * y * (x + y) := by
  sorry