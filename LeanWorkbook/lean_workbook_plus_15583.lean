import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_15583 (α β : ℝ) : (sin α)^2 + (sin β)^2 + 1 ≥ sin α + sin β + sin α * sin β := by
  sorry