import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_theorem (a b c : ℝ) : 4 * (a^2 * b^2 + b^2 * c^2 + c^2 * a^2) = 4 * a^2 * b^2 + 4 * b^2 * c^2 + 4 * c^2 * a^2 := by
  sorry