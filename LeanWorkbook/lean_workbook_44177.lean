import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_44177  (a b c d : ℝ) :
  (a^2+1) * (b^2+1) * (c^2+1) * (d^2+1) ≥
  ((a+b)^2 + (a * b - 1)^2) * ((c+d)^2 + (c * d - 1)^2) := by
  sorry