import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_66830 (a₁ a₂ : ℝ) : (6*a₁ + a₂ = -27 ∧ 3*a₁ + 2*a₂ = -2) ↔ a₁ = -52/9 ∧ a₂ = 23/3 := by
  sorry