import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_59092 (p q r s : ℕ) : (p, q, r, s) = (0, 0, 0, 0) → p + q + r + s = 0 := by
  sorry