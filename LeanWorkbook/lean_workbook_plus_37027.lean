import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_37027 (a b c : ℝ) : 16 * (a ^ 2 + b ^ 2 + a * b) * (b ^ 2 + c ^ 2 + b * c) ≥ 9 * (a + b) ^ 2 * (b + c) ^ 2 := by
  sorry