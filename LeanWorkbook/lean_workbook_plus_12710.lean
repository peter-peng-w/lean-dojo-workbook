import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_12710 (a b c : ℝ) (ha : 0 < a) (hb : 0 < b) (hc : 0 < c) : (b^2 - b*c + c^2)*(b - c)^2 + (c^2 - c*a + a^2)*(c - a)^2 + (a^2 - a*b + b^2)*(a - b)^2 ≥ 0 := by
  sorry