import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_54528 : ∀ a b c : ℝ, a * b * c = 1 → a ^ 4 + b ^ 4 + c ^ 4 + 3 * (a ^ 2 * b ^ 2 + b ^ 2 * c ^ 2 + a ^ 2 * c ^ 2) ≥ 2 * (a ^ 3 * b + b ^ 3 * c + c ^ 3 * a) + 2 * (a * b ^ 3 + b * c ^ 3 + c * a ^ 3) := by
  sorry