import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_39065 (a b c d : ℝ) (ha : 0 < a) (hb : 0 < b) (hc : 0 < c) (hd : 0 < d) : (3 * a / (a + 2 * b + 2 * c + 2 * d))^(3 / 4) + (4 * b / (b + 3 * c + 3 * d + 3 * a))^(2 / 3) + (6 * c / (c + 5 * d + 5 * a + 5 * b))^(3 / 5) + (d / (a + b + c))^(1 / 2) ≥ 2 := by
  sorry