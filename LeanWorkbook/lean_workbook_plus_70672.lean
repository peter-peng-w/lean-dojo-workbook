import LeanWorkbook.LeanWorkbookImport

open BigOperators

open Nat

open Real

open Rat

theorem lean_workbook_plus_70672 (x y : ℝ) : cos x * cos y = 1 / 2 * (cos (x + y) + cos (x - y)) := by
  sorry